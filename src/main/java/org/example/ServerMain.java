package org.example;

import org.example.clientServer.GUI.ServerForGUI;
import org.example.clientServer.ServerInterface;

public class ServerMain {
    public static void main(String[] args) {

        ServerInterface server = new ServerForGUI();
    }
}
